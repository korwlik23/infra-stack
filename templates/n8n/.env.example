# ── __PROJECT__ (n8n) ────────────────────────────────
APP_DOMAIN=n8n.example.com
TZ=Asia/Bangkok

# openssl rand -hex 24  — ห้ามเปลี่ยนหลังใช้งานแล้ว (credentials จะถอดรหัสไม่ได้)
N8N_ENCRYPTION_KEY=CHANGE_ME

DB_DATABASE=n8n_db
DB_USERNAME=infra
DB_PASSWORD=CHANGE_ME
