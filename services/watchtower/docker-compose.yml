services:
  watchtower:
    image: containrrr/watchtower:latest
    container_name: watchtower
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - TZ=${TZ:-Asia/Bangkok}
      # Watchtower default ใช้ Docker API 1.25 ซึ่ง Docker 29+ เลิกรองรับ
      # ("client version 1.25 is too old") — บังคับเวอร์ชันใหม่กว่า
      - DOCKER_API_VERSION=1.40
      # อัปเดตเฉพาะ container ที่ติด label opt-in เท่านั้น (ปลอดภัยกว่าอัปเดตหมด)
      - WATCHTOWER_LABEL_ENABLE=true
      - WATCHTOWER_CLEANUP=true
      # เช็คทุกวัน 04:00
      - WATCHTOWER_SCHEDULE=0 0 4 * * *
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

networks: {}
